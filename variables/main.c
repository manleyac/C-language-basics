#include <stdio.h>
#include <stdlib.h>

int main() {
   //bracket used after variable name for strings instead of a char
   char name[] = "Andrew";
   int age = 24;
   printf("%s is %i\n", name, age);
   return 0;
}